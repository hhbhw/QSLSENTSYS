# Frontend (React + Vite)

## 1. Install

```bash
npm install
```

## 2. Configure

Create `.env` and set backend base URL if needed:

```bash
VITE_API_BASE=http://localhost:8000/api/v1
```

## 3. Run

```bash
npm run dev -- --host 0.0.0.0 --port 5173
```
